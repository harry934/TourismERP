{
  "name": "lamai-tourism-erp",
  "private": true,
  "description": "Internal enquiry-to-operations workspace for Lamai Africa Safaris.",
  "scripts": {
    "dev": "npm --prefix web run dev",
    "build:ui": "npm --prefix web run build && node scripts/finalize-ui.js"
  }
}
